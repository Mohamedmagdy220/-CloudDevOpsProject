apiVersion: v1
kind: PersistentVolume
metadata:
  name: my-db-pv
  namespace: ivolve
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: "/mnt/data/db"  # تأكد المجلد ده موجود على النود
